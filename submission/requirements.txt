# ポリシーサーバーの依存（必須、削除しないでください）
fastapi>=0.68
uvicorn>=0.15
msgpack>=1.0
numpy>=1.19
requests>=2.28

# LeRobot v0.4.4 SmolVLA推論に必要な依存関係
# 注意: バンドルされたlerobotディレクトリをsys.pathで読み込むため、ここではlerobotの依存のみ記載

# transformers関連（LeRobotの依存に含まれるが、バージョンを明示）
transformers>=4.57.1,<5.0.0
num2words>=0.5.14,<0.6.0
accelerate>=1.7.0,<2.0.0
safetensors>=0.4.3,<1.0.0

# Hugging Face
huggingface-hub>=0.34.2,<0.36.0

# PyTorch (LeRobot v0.4.4互換バージョン)
# 注意: 完全固定ではなく範囲指定にすることで、採点サーバーに同等バージョンが
# 既インストールの場合に pip が再インストール（CUDA libの上書き）を試みないようにする
torch>=2.10.0,<2.12.0
torchvision>=0.25.0,<0.27.0

# データ処理
datasets>=4.0.0,<5.0.0
diffusers>=0.27.2,<0.36.0
jsonlines>=4.0.0,<5.0.0
deepdiff>=7.0.1,<9.0.0

# システム依存
pyserial>=3.5,<4.0
av>=15.0.0,<16.0.0

# その他の基本依存
Pillow>=9.0
opencv-python>=4.5
einops>=0.6.0
gymnasium>=0.28.0
draccus==0.10.0
packaging>=24.2,<26.0
imageio[ffmpeg]>=2.34.0,<3.0.0
termcolor>=2.4.0,<4.0.0
setuptools>=71.0.0,<81.0.0
